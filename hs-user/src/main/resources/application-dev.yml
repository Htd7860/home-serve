spring:
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    url: jdbc:mysql://localhost:3306/home_serve?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 502938

mybatis-plus:
  type-aliases-package: com.qw
  global-config:
    db-config:
      id-type: auto
rocketmq:
  name-server: localhost:9876
  producer:
    group: order-producer-group

ws:
  node:
    id: 127.0.0.1:8080