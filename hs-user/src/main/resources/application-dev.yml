spring:
  data:
    redis:
      host: localhost
      port: 6379
  datasource:
    url: jdbc:mysql://localhost:3306/home_serve?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 502938

    mybatis-plus:
      mapper-locations: classpath*:/com.qw.admin.mapper/**/*.xml
      type-aliases-package: com.qw
      global-config:
        db-config:
          id-type: auto

